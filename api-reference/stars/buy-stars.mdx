---
title: "Buy Stars"
api: "POST /v1/stars/buy"
description: "Purchase Telegram Stars for a user"
---

Buy stars for a Telegram user by their username.

### Headers

<ParamField header="Authorization" type="string" required>
  Bearer token with your API key
</ParamField>

### Body

<ParamField body="quantity" type="integer" required>
  Number of stars to purchase
</ParamField>

<ParamField body="username" type="string" required>
  Telegram username of the recipient (without @)
</ParamField>

<ParamField body="show_sender" type="boolean" default="false">
  Whether to show the sender's information to the recipient
</ParamField>

### Response

<ResponseField name="success" type="boolean">
  Whether the purchase was successful
</ResponseField>

<ResponseField name="transaction_hash" type="string">
  TON blockchain transaction hash
</ResponseField>

<RequestExample>

```bash cURL
curl --request POST \
  --url https://fragment-api.up.railway.app/v1/stars/buy \
  --header 'Authorization: Bearer YOUR_API_KEY' \
  --header 'Content-Type: application/json' \
  --data '{
    "quantity": 1000,
    "username": "johndoe",
    "show_sender": false
  }'
```

```python Python
import requests

url = "https://fragment-api.up.railway.app/v1/stars/buy"
headers = {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
}
payload = {
    "quantity": 1000,
    "username": "johndoe",
    "show_sender": False
}

response = requests.post(url, headers=headers, json=payload)
print(response.json())
```

```javascript JavaScript
const response = await fetch('https://fragment-api.up.railway.app/v1/stars/buy', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    quantity: 1000,
    username: 'johndoe',
    show_sender: false
  })
});

const data = await response.json();
console.log(data);
```

</RequestExample>

<ResponseExample>

```json 200 Response
{
  "success": true,
  "transaction_hash": "abc123def456..."
}
```

</ResponseExample>
